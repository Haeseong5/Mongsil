<?xml version="1.0" encoding="utf-8"?>

<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/pagerFragment">

    <fragment
        android:id="@+id/pagerFragment"
        android:name="com.cashproject.mongsil.ui.main.PagerFragment"
        android:label="@string/calendar"
        tools:layout="@layout/fragment_pager">
        <action
            android:id="@+id/action_pager_to_home"
            app:destination="@id/homeFragment"/>
        <action
            android:id="@+id/action_locker_to_home"
            app:destination="@id/homeFragment"/>
        <argument
            android:name="docId"
            app:argType="string"/>
        <argument
            android:name="image"
            app:argType="string"/>
    </fragment>

    <fragment
            android:id="@+id/calendarFragment"
            android:name="com.cashproject.mongsil.ui.calendar.CalendarFragment"
            android:label="@string/calendar"
            tools:layout="@layout/fragment_calendar">
        <action
            android:id="@+id/action_calendar_to_home"
            app:destination="@id/homeFragment"/>
        <argument
            android:name="docId"
            app:argType="string"/>
        <argument
            android:name="image"
            app:argType="string"/>
    </fragment>

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.cashproject.mongsil.ui.home.HomeFragment"
        android:label="@string/saying"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_home_to_calendar"
            app:destination="@id/calendarFragment"/>
        <action
            android:id="@+id/action_home_to_locker"
            app:destination="@id/lockerFragment"/>
    </fragment>

    <fragment
        android:id="@+id/lockerFragment"
        android:name="com.cashproject.mongsil.ui.locker.LockerFragment"
        android:label="@string/locker"
        app:popUpToInclusive="true"
        app:popUpTo="@id/homeFragment"
        tools:layout="@layout/fragment_locker">
        <action
            android:id="@+id/action_locker_to_home"
            app:destination="@id/homeFragment"/>
        <argument
            android:name="docId"
            app:argType="string"/>
        <argument
            android:name="image"
            app:argType="string"/>
    </fragment>


</navigation>
